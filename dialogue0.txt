1, inventory(ID)==0 && alternate==0, alternate=1, "Hmm, his ID must be around here somewhere"
1, inventory(ID)==0 && alternate==1, alternate=0, "I need to find his ID if it's around"
1, inventory(ID)==1 && self.location==engineroomID && spoken==0, spoken=1, "Well, I give up. Maybe it’ll turn up later. Guess I’ll head on up to the bridge"
1, inventory(ID)==1 && self.location==engineroomID && spoken==1, "I’ll just look one more time..."
1, inventory(ID)==1, "Go back to your duties, robot."
